# FTP Deployment Configuration
# Copy this file to .env and fill in your actual FTP credentials

# FTP Server Details
FTP_HOST=your-domain.com
FTP_PORT=21
FTP_USER=your-ftp-username@your-domain.com
FTP_PASSWORD=your-ftp-password

# Use secure FTP (FTPS) - set to 'true' to enable, 'false' to disable
FTP_SECURE=false

# Target directory on the server (where files will be uploaded)
# Usually 'giftcard' if FTP home is set to public_html/giftcard
FTP_TARGET_DIR=giftcard

# Optional: API Keys (if needed)
# GEMINI_API_KEY=your-api-key-here
